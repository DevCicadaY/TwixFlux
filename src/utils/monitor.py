import time  # Импортируем модуль для работы с временем
import os  # Импортируем модуль для работы с операционной системой
import signal  # Импортируем модуль для работы с сигналами
import psutil  # Импортируем модуль для работы с процессами
import asyncio  # Импортируем модуль для асинхронного выполнения
import traceback  # Импортируем модуль для работы с трассировками ошибок
import sys  # Импортируем модуль для работы с системой
from src.utils.logger import logger  # Импортируем логгер для записи логов
import socket  # Импортируем модуль для работы с сетевыми сокетами

# Время в секундах, по прошествии которого программа завершит себя при простое
MAX_IDLE_TIME = 60  # Устанавливаем максимальное время простоя

# Словарь для отслеживания времени последней активности
last_activity_time = time.time()  # Инициализируем время последней активности

# Список портов для отслеживания
PORTS_TO_MONITOR = [8080]  # Замените на ваши порты для мониторинга

def check_ports_closed():
    """
    Проверяет, закрыты ли все порты из списка.
    Если хотя бы один порт открыт, возвращает False.
    Если все порты закрыты, возвращает True.
    """
    for port in PORTS_TO_MONITOR:  # Проходим по всем портам для мониторинга
        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:  # Создаём сокет для проверки соединения
            result = s.connect_ex(('localhost', port))  # Проверка соединения с портом
            if result == 0:  # Порт открыт, возвращаем False
                logger.warning(f"🚨 Порт {port} открыт.")  # Логируем открытие порта
                return False
    logger.info("✅ Все порты закрыты.")  # Логируем, если все порты закрыты
    return True  # Возвращаем True, если все порты закрыты

async def monitor_ports():
    """
    Мониторит порты и завершает программу, если они все закрыты.
    """
    while True:  # Запускаем бесконечный цикл для мониторинга портов
        if check_ports_closed():  # Проверяем, закрыты ли порты
            logger.info("✅ Все порты закрыты. Завершаю программу...")  # Логируем завершение
            os.kill(os.getpid(), signal.SIGINT)  # Завершаем программу, отправляя сигнал SIGINT
            break  # Выходим из цикла
        await asyncio.sleep(1)  # Ждём 1 секунду перед следующей проверкой

async def is_process_hanging(pid, timeout=10):
    """
    Проверяет, завис ли процесс. 🛑
    Если процесс неактивен более `timeout` секунд, возвращает True.
    """
    try:
        logger.debug(f"🔍 Проверка состояния процесса с PID {pid}...")  # Логируем начало проверки процесса
        proc = psutil.Process(pid)  # Получаем процесс по PID
        cpu_usage = proc.cpu_percent(interval=timeout)  # Измеряем загрузку процессора
        if cpu_usage == 0.0:  # Если процесс не использует CPU
            logger.warning(f"⚠️ Процесс {pid} неактивен, потребление CPU равно 0.")  # Логируем зависание
            return True  # Возвращаем True, если процесс завис
        logger.debug(f"✅ Процесс {pid} активен, потребление CPU: {cpu_usage}.")  # Логируем активность процесса
        return False  # Возвращаем False, если процесс активен
    except psutil.NoSuchProcess:  # Если процесс не найден
        logger.error(f"❌ Процесс с PID {pid} не найден.")  # Логируем ошибку
        return False  # Возвращаем False, так как процесс не найден

async def monitor_process(pid, check_interval=5):
    """
    Мониторит процесс по PID. 👀
    Отправляет сигнал SIGINT, если процесс завис.
    """
    logger.info(f"🔧 Запуск мониторинга процесса с PID {pid}...")  # Логируем начало мониторинга
    while True:  # Запускаем бесконечный цикл для мониторинга процесса
        if await is_process_hanging(pid):  # Проверяем, завис ли процесс
            logger.warning(f"🛑 Процесс {pid} завис. Отправляю сигнал SIGINT...")  # Логируем зависание
            os.kill(pid, signal.SIGINT)  # Отправляем сигнал SIGINT для завершения процесса
            break  # Выходим из цикла
        await asyncio.sleep(check_interval)  # Ждём интервал перед следующей проверкой

async def monitor_idle_time():
    """
    Следит за временем простоя программы и завершает ее при длительном бездействии. ⏱️
    """
    logger.info("🔄 Запуск мониторинга времени простоя...")  # Логируем запуск мониторинга простоя
    while True:  # Запускаем бесконечный цикл для отслеживания времени простоя
        idle_time = time.time() - last_activity_time  # Вычисляем время простоя
        logger.debug(f"⏳ Время простоя программы: {idle_time} секунд.")  # Логируем время простоя
        if idle_time > MAX_IDLE_TIME:  # Если время простоя больше максимального
            logger.warning(f"⚠️ Программа простаивает более {MAX_IDLE_TIME} секунд. Завершаю...")  # Логируем предупреждение
            os.kill(os.getpid(), signal.SIGINT)  # Завершаем программу
            break  # Выходим из цикла
        await asyncio.sleep(1)  # Ждём 1 секунду перед следующей проверкой

async def check_shutdown():
    """
    Функция-выключатель, отслеживает условие завершения программы. 🛑
    Например, завершение при получении команды от пользователя.
    """
    logger.info("🛠️ Запуск мониторинга команды завершения программы...")  # Логируем запуск мониторинга завершения
    while True:  # Запускаем бесконечный цикл для отслеживания команды завершения
        shutdown_input = input("Введите 'exit' для завершения программы: ").strip().lower()  # Ожидаем ввода пользователя
        if shutdown_input == 'exit':  # Если введена команда "exit"
            logger.info("✅ Получена команда на завершение программы. Выключаю...")  # Логируем команду завершения
            os.kill(os.getpid(), signal.SIGINT)  # Завершаем программу
            break  # Выходим из цикла
        await asyncio.sleep(1)  # Ждём 1 секунду перед следующей проверкой

async def monitor_python_exceptions():
    """
    Ловит все необработанные исключения и завершает программу. 🐍❌
    """
    logger.info("⚠️ Запуск мониторинга исключений Python...")  # Логируем запуск мониторинга исключений
    def exception_handler(exc_type, exc_value, exc_tb):  # Обработчик исключений
        logger.error("❌ Произошла ошибка Python:")  # Логируем ошибку
        traceback.print_exception(exc_type, exc_value, exc_tb)  # Печатаем трассировку ошибки
        logger.error("⚠️ Завершаю программу...")  # Логируем завершение программы
        os.kill(os.getpid(), signal.SIGINT)  # Завершаем программу
    
    sys.excepthook = exception_handler  # Устанавливаем обработчик для необработанных исключений

async def main():
    """
    Основная функция программы. 🚀
    Запускает мониторинг зависания процесса и выполняет основную логику.
    """
    logger.info("🚀 Запуск основной программы...")  # Логируем запуск основной программы
    pid = os.getpid()  # Получаем PID текущего процесса

    # Запуск асинхронного мониторинга зависания
    monitor_process_task = asyncio.create_task(monitor_process(pid))
    logger.info("👀 Запущен мониторинг зависания процесса...")  # Логируем запуск мониторинга

    # Запуск асинхронного мониторинга простоя
    monitor_idle_time_task = asyncio.create_task(monitor_idle_time())
    logger.info("⏳ Запущен мониторинг простоя...")  # Логируем запуск мониторинга

    # Запуск асинхронной функции для проверки выключения
    shutdown_task = asyncio.create_task(check_shutdown())
    logger.info("🔒 Запущен мониторинг выключения программы...")  # Логируем запуск мониторинга

    # Запуск мониторинга исключений Python
    monitor_exceptions_task = asyncio.create_task(monitor_python_exceptions())
    logger.info("⚠️ Запущен мониторинг необработанных исключений...")  # Логируем запуск

    # Запуск мониторинга портов
    monitor_ports_task = asyncio.create_task(monitor_ports())
    logger.info("🌐 Запущен мониторинг портов...")  # Логируем запуск мониторинга

    # Основной цикл программы (должен выполнять работу, например, обрабатывать запросы)
    try:
        while True:  # Запускаем основной цикл работы программы
            logger.info("🛠️ Программа работает...")  # Логируем выполнение работы
            await asyncio.sleep(1)  # Программа выполняет работу

            # Если программа выполняет работу, обновляем время последней активности
            global last_activity_time
            last_activity_time = time.time()  # Обновляем время последней активности

            # Добавьте сюда основную логику программы (например, обработка данных, выполнение задач)

    except KeyboardInterrupt:  # Обработка завершения по Ctrl+C
        logger.info("❌ Программа завершена по Ctrl+C.")  # Логируем завершение по Ctrl+C
    except Exception as e:  # Обработка других ошибок
        logger.error(f"❌ Произошла ошибка: {e}")  # Логируем ошибку
    finally:  # Заключительный блок
        logger.info("✅ Программа завершена.")  # Логируем завершение программы

# Запуск основного цикла событий
if __name__ == "__main__":  # Проверка, если это основной модуль
    asyncio.run(main())  # Запуск асинхронной функции main